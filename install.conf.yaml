- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:
      force: true
      path: gitconfig

- shell:
    -
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true